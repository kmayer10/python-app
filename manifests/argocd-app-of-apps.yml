apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: appofapps
  namespace: argocd
spec:
  destination:
    name: in-cluster

  project: default

  source:
    repoURL: git@github.com:kmayer10/python-app.git
    path: manifests/apps
    targetRevision: argocd-demo